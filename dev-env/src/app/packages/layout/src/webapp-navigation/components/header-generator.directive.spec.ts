import { BATWebappNavigationHeaderGeneratorDirective } from './header-generator.directive';

describe('BATWebappNavigationHeaderGeneratorDirective', () => {
  it('should create an instance', () => {
    const directive = new BATWebappNavigationHeaderGeneratorDirective();
    expect(directive).toBeTruthy();
  });
});
