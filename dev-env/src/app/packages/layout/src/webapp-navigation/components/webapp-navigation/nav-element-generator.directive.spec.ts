import { BATNavElementGeneratorDirective } from './nav-element-generator.directive';

describe('BATNavElementGeneratorDirective', () => {
  it('should create an instance', () => {
    const directive = new BATNavElementGeneratorDirective();
    expect(directive).toBeTruthy();
  });
});
