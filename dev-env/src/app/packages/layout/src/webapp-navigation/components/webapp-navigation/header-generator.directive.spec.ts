import { BATHeaderGeneratorDirective } from './header-generator.directive';

describe('BATHeaderGeneratorDirective', () => {
  it('should create an instance', () => {
    const directive = new BATHeaderGeneratorDirective();
    expect(directive).toBeTruthy();
  });
});
